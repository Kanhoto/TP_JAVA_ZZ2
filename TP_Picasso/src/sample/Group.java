package sample;

import javafx.scene.shape.Rectangle;

public class Group {
    Rectangle r = new Rectangle();

    r.setX(Math.random()*50);
    r.setY(Math.random()*50);
    r.setWidth(Math.random()*200);
    r.setHeight(Math.random()*100);
    r[i].setArcWidth(20);
    r[i].setArcHeight(20);
}
